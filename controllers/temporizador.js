const con = require('../database/db');

/*exports.tiempo = (res,req) =>{
    conexion.query('SELECT * FROM predicciones ORDER BY idRegistro DESC LIMIT 1;',function (error, result, fields) {
        if(error) throw error;
        console.log(result);
    });
}*/

/*con.connect(function(err) {
    if (err) throw err;
    //Select all customers and return the result object:
    con.query("SELECT * FROM registros", function (err, result, fields) {
      if (err) throw err;
      console.log(result);
    });
  });*/